# 作業ログ 2026-05-29 — 層1「検索できる日記」完成

> 未来の自分へ。この日に何をやって、今どこまで動いていて、次に何をすればいいか。
> 間が空いても、このログの「再開手順」から入れば続きをやれる。

---

## この日の到達点(一言)

**層1「検索できる日記」が動く状態になった。**
過去の全日記を保存・キーワード検索・意味検索でき、新しい日記も保存できる。
最初に定義した完成形(「日記を保存して検索する＋新しく書いて保存する」)を達成。
当初の夢だった意味検索(言葉が違っても意味で引く)まで届いた。

---

## 今動いているもの

| 機能 | 状態 | 呼び方(例) |
|---|---|---|
| Raw層DB(SQLAlchemy) | ✅ 424件 | app/db/raw.sqlite |
| 新規保存(id/hash自動生成) | ✅ | POST /entries/ |
| キーワード検索 | ✅ | GET /entries/search?q=劣等感 |
| 期間検索(時間軸モードの土台) | ✅ | GET /entries/range?start=2022-01-01&end=2022-12-31 |
| 意味検索(bge-m3+ChromaDB) | ✅ | GET /entries/semantic?q=孤独で誰にも理解されない気持ち |
| 暗号化バックアップ | ✅(zipのscripts) | backup_crypto.py |

意味検索の実証: 「孤独で誰にも理解されない気持ち」で検索 → 「寂寥感」「友達がいない事に嘆く」等、
その単語を含まない日記が意味で繋がってヒットした。キーワード検索では不可能だった検索ができている。

---

## この日やった作業の流れ(何を触ったか)

1. **既存プロジェクトの発見**: ~/projects/naru-ai に過去の自分が作ったSQLAlchemy版バックエンドがあった
   (main.py, db/models.py, db/database.py, schemas/raw.py, crud/raw.py, api/raw.py, core/config.py)。
   到達点は「保存APIの骨格 + テストデータ1件」。services/processing.py は空。
2. **統合方針の決定**: 過去のSQLAlchemy設計が優れている(PostgreSQL移行に強い)ので土台として活かし、
   そこに機能を足す方針にした。生sqlite3版で上書きはしない。
3. **触ったファイル**:
   - db/models.py: カラム追加(reaction_*, created_at, chapter系, valence/arousal/entry_type)
   - app/db/raw.sqlite: 423件入りDBに差し替え + ALTER TABLEで valence/arousal/entry_type 追加
   - schemas/raw.py: id/hash/date を任意に(送られたら使う、無ければ生成)
   - utils/hash.py: 【新規】SHA-256生成
   - crud/raw.py: create に id/hash自動生成+衝突回避を実装。search_by_keyword / list_by_date_range 追加
   - api/raw.py: /search, /range, /semantic エンドポイント追加
   - services/vector.py: 【新規】bge-m3 + ChromaDB
   - scripts/build_vectors.py: 【新規】424件をChromaDBへ流し込み(実行済み)
4. **uv add sentence-transformers chromadb** でライブラリ追加
5. **build_vectors.py 実行** → 424件(空7件除く)をベクトル化、GPU(RTX 4060 Ti)で処理

## ハマった点と対処(同じ轍を踏まないため)
- 500エラー `no such column: raw_entries.valence`
  → models.pyにvalenceを書いたがDB実体に無かった。ALTER TABLEで3カラム追加して解決。
- 件数が425になった(424のはず)
  → 500エラー時もINSERTは通っていて重複していた。古い1件をDELETEして424に。
  → 教訓: INSERTは通ってもその後のrefresh/レスポンス整形で落ちると「入ったがエラー」になる。

---

## 構成(現在)

```
~/projects/naru-ai/
├── app/
│   ├── main.py              # prefix="/entries" でルーター登録
│   ├── core/config.py       # DATABASE_URL = sqlite:///./app/db/raw.sqlite
│   ├── db/
│   │   ├── database.py      # SQLAlchemy engine/session
│   │   ├── models.py        # RawEntry (15カラム)
│   │   ├── raw.sqlite       # 424件 (gitignore対象にすべき!)
│   │   └── chroma_db/       # ベクトルDB (gitignore対象にすべき!)
│   ├── schemas/raw.py       # 入力は任意多め/出力は確定
│   ├── crud/raw.py          # create + search + range
│   ├── api/raw.py           # /, /search, /range, /semantic
│   ├── utils/hash.py        # SHA-256
│   └── services/
│       ├── vector.py        # bge-m3 + ChromaDB
│       └── processing.py    # 空(層2で書く)
├── scripts/
│   └── build_vectors.py     # 424件→ChromaDB
└── (frontend/ は未着手)
```

---

## 再開手順(次に開いた時これをやる)

```bash
# 1. バックエンド起動
cd ~/projects/naru-ai
uv run uvicorn app.main:app --reload

# 2. ブラウザで操作画面
#    http://localhost:8000/docs

# 3. 動作確認(意味検索。初回はbge-m3ロードで数十秒)
#    http://localhost:8000/entries/semantic?q=将来が不安
```

新しい日記を追加したら、それを意味検索可能にするには ChromaDB にも入れる必要がある。
→ 保存API(create)内で vector.add_entry を呼ぶようにすれば自動化できる(下の「次の課題」参照)。

---

## 次にやること(優先度順)

### すぐやるべき小修正
- [ ] **保存時にChromaDBにも自動登録**: 今の crud.create_raw_entry は SQLite に入れるだけ。
      新規日記が意味検索に出てこない。create の最後に vector.add_entry を呼ぶ。
- [ ] **.gitignore 確認**: app/db/raw.sqlite と app/db/chroma_db/ を除外(生の日記を公開しない)。
      既存の .gitignore に追記する。
- [ ] **.env がGitHubにコミットされていないか確認**(以前の指摘)。秘密情報があれば履歴から削除。

### 層1の仕上げ
- [ ] **Next.js UI**: 入力フォーム + 検索画面。zipの frontend/ を参考に。
      今はブラウザURL直打ち/docsで操作している部分を画面にする。
- [ ] semantic検索に response_model を付けて型を整える(今は型指定なし)。
- [ ] タイムゾーン: id生成の時刻がUTC基準。日本時間にしたいなら調整。

### 層2(思考パートナー) — 当面の楽しみとして取っておく
- [ ] Ollama + Phi-3 mini で entry_type 判定 (services/processing.py を書く)
- [ ] Mistral/Qwen 7B で valence/arousal 推定 → DBの該当カラムを埋める
- [ ] Episode層の構築(感情の動きのまとまり)
- [ ] DTWによる「心の動きの軌跡」検索 ← 本プロジェクトの独自性
- [ ] AI壁打ち(出力を 事実/仮説/別解釈/次の問い に分離)

---

## 今日確定した設計判断(tech_decisions.md に転記推奨)
- 判断⑦: source に creative を追加(創作を感情推定から除外)。002は感情が出ているのでdiaryに。
- 判断⑧: 暗号化クラウドバックアップのハイブリッド(vol.1判断①を更新)。
  AES-256-GCM + argon2id。パスフレーズは頭+紙、Googleには置かない(鍵とデータの事業者分離)。
- 統合判断: 過去のSQLAlchemy版を土台に。生sqlite3版では上書きしない(ORMの方がPostgreSQL移行に強い)。
- 発見: プロジェクトは層1(検索日記)と層2(思考パートナー)に分解できる。層1から作る戦略。
```
